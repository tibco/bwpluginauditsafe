package com.tibco.bw.palette.tcta.model.tcta;

import com.tibco.bw.palette.tcta.model.tcta.TctaPackage;
import com.tibco.bw.palette.tcta.model.tcta.UpsertRow;
import com.tibco.bw.validation.process.ActivityConfigurationValidator;
import com.tibco.bw.validation.process.ActivityValidationContext;
import com.tibco.bw.palette.tcta.model.utils.Messages;
import com.tibco.bw.palette.tcta.model.utils.MessageCode;
import org.eclipse.osgi.util.NLS;

public class UpsertRowValidator implements ActivityConfigurationValidator {
	/**
	 * <!-- begin-custom-doc -->
	 *
	 * <!-- end-custom-doc -->
	 * @generated
	 */
	@Override
	public void validateBWActivityConfiguration(ActivityValidationContext context) {
//	    UpsertRow model =(UpsertRow) context.getActivityConfigurationModel();
//
//	    String spinnerModul = context.getAttributeBindingPropertyName("spinner");
//		if(spinnerModul == null || "".equals(spinnerModul)){
//		    Integer spinner = model.getSpinner();
//		    if(spinner == null || "".equals(spinner)) {
//		        String message = ""; //$NON-NLS-1$
//		        message = NLS.bind(Messages.PALETTE_PARAMETER_VALUE_INVALID, new String[] {"Spinner"});
//		        context.createError(message, null, MessageCode.PARAMETER_NOT_SPECIFIED, TctaPackage.Literals.UPSERT_ROW__SPINNER);
//		    }
//		}
//	    String checkBoxModul = context.getAttributeBindingPropertyName("checkBox");
//		if(checkBoxModul == null || "".equals(checkBoxModul)){
//		    String checkBox = model.getCheckBox();
//		    if(checkBox == null || "".equals(checkBox)) {
//		        String message = ""; //$NON-NLS-1$
//		        message = NLS.bind(Messages.PALETTE_PARAMETER_VALUE_INVALID, new String[] {"CheckBox"});
//		        context.createError(message, null, MessageCode.PARAMETER_NOT_SPECIFIED, TctaPackage.Literals.UPSERT_ROW__CHECK_BOX);
//		    }
//		}
//	    String comboViewerModul = context.getAttributeBindingPropertyName("comboViewer");
//		if(comboViewerModul == null || "".equals(comboViewerModul)){
//		    String comboViewer = model.getComboViewer();
//		    if(comboViewer == null || "".equals(comboViewer)) {
//		        String message = ""; //$NON-NLS-1$
//		        message = NLS.bind(Messages.PALETTE_PARAMETER_VALUE_INVALID, new String[] {"ComboViewer"});
//		        context.createError(message, null, MessageCode.PARAMETER_NOT_SPECIFIED, TctaPackage.Literals.UPSERT_ROW__COMBO_VIEWER);
//		    }
//		}
//	    String textBoxModul = context.getAttributeBindingPropertyName("textBox");
//		if(textBoxModul == null || "".equals(textBoxModul)){
//		    String textBox = model.getTextBox();
//		    if(textBox == null || "".equals(textBox)) {
//		        String message = ""; //$NON-NLS-1$
//		        message = NLS.bind(Messages.PALETTE_PARAMETER_VALUE_INVALID, new String[] {"TextBox"});
//		        context.createError(message, null, MessageCode.PARAMETER_NOT_SPECIFIED, TctaPackage.Literals.UPSERT_ROW__TEXT_BOX);
//		    }
//		}
		// begin-custom-code
	    // end-custom-code
  	}
}
